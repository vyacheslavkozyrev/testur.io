# IMPORTANT: This file is a template. DO NOT commit this file with real values.
# Before deploying, copy this file to api.secret.yaml (which is git-ignored),
# substitute all <REPLACE> placeholders with actual values, and apply with:
#   kubectl apply -f infra/k8s/base/api/api.secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: testurio
type: Opaque
stringData:
  Cosmos__ConnectionString: "<REPLACE>"
  ServiceBus__ConnectionString: "<REPLACE>"
  Stripe__SecretKey: "<REPLACE>"
  Stripe__WebhookSecret: "<REPLACE>"
  AzureAdB2C__ClientSecret: "<REPLACE>"
  Anthropic__ApiKey: "<REPLACE>"
