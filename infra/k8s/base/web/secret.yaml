# IMPORTANT: This file is a template. DO NOT commit this file with real values.
# Before deploying, copy this file to web.secret.yaml (which is git-ignored),
# substitute all <REPLACE> placeholders with actual values, and apply with:
#   kubectl apply -f infra/k8s/base/web/web.secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: web-secret
  namespace: testurio
type: Opaque
stringData:
  NEXTAUTH_SECRET: "<REPLACE>"
  NEXTAUTH_URL: "<REPLACE>"
