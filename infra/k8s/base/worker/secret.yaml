# IMPORTANT: This file is a template. DO NOT commit this file with real values.
# Before deploying, copy this file to worker.secret.yaml (which is git-ignored),
# substitute all <REPLACE> placeholders with actual values, and apply with:
#   kubectl apply -f infra/k8s/base/worker/worker.secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: worker-secret
  namespace: testurio
type: Opaque
stringData:
  Cosmos__ConnectionString: "<REPLACE>"
  ServiceBus__ConnectionString: "<REPLACE>"
  Anthropic__ApiKey: "<REPLACE>"
  AzureAiSearch__ApiKey: "<REPLACE>"
