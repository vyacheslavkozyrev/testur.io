# name: Release Please

# on:
#   push:
#     branches:
#       - develop

# permissions:
#   contents: write       # push tags, update changelog, bump package.json
#   pull-requests: write  # open the Release PR

# jobs:
#   release-please:
#     name: Release Please
#     runs-on: ubuntu-latest
#     outputs:
#       ui-release-created: ${{ steps.release.outputs['source/Testurio.Web--release_created'] }}
#       ui-tag-name: ${{ steps.release.outputs['source/Testurio.Web--tag_name'] }}
#       ui-version: ${{ steps.release.outputs['source/Testurio.Web--version'] }}
#     steps:
#       - name: Run release-please
#         id: release
#         uses: googleapis/release-please-action@v5
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           config-file: .github/release-please/release-please-config.json
#           manifest-file: .github/release-please/.release-please-manifest.json

#       # Auto-merge the Release PR using a fine-grained PAT.
#       # GITHUB_TOKEN-merged PRs do not trigger downstream tag-based workflows.
#       # PAT scope required: contents:write, pull-requests:write on this repo only.
#       # Phase 2/3: remove auto-merge to require manual merge before promoting to higher environments.
#       - name: Auto-merge Release PR
#         if: ${{ steps.release.outputs['source/Testurio.Web--prs_created'] }}
#         env:
#           GH_TOKEN: ${{ secrets.RELEASE_PLEASE_TOKEN }}
#         run: |
#           gh pr merge \
#             --auto \
#             --squash \
#             "${{ steps.release.outputs['source/Testurio.Web--pr'] }}"
