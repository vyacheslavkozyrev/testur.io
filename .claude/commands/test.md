---
description: Verifies a implemented feature by validating acceptance criteria against the running system.
---

The feature number is passed as the command argument (`$ARGUMENTS`).

Spawn the `test` agent (`.claude/agents/test.md`) with the feature number as the prompt. Wait for it to complete and surface its output to the user.
