---
description: Reviews implemented feature code against its specification and implementation plan.
---

The feature number is passed as the command argument (`$ARGUMENTS`).

Spawn the `review` agent (`.claude/agents/review.md`) with the feature number as the prompt. Wait for it to complete and surface its output to the user.
