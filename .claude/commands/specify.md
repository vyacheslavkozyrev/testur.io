---
description: Read feature description and generate detailed specification
---

The feature number is passed as the command argument (`$ARGUMENTS`).

Spawn the `specify-and-plan` agent (`.claude/agents/specify-and-plan.md`) with the feature number as the prompt. Wait for it to complete and surface its output to the user.
